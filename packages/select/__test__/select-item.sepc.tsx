import { mount } from '@vue/test-utils';
import { LSelectItem } from '..';

describe('select-item', () => {
	it('select-item mount', () => {
		mount(LSelectItem);
	});

	it.todo('select-item disabled', () => {});
});
